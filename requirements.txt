# Manipulación de datos
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # backend de parquet: la cohorte de 1,5M filas en CSV es inmanejable

# Visualización
matplotlib>=3.8
seaborn>=0.13
scienceplots>=2.1       # estilo visual único del proyecto (src/viz.py)
yellowbrick>=1.5        # diagnósticos de clasificación (notebook 08)

# Modelado
scikit-learn>=1.4
lightgbm>=4.3
xgboost>=2.0
catboost>=1.2            # quinto modelo tabular, campeón final
shap>=0.44             # interpretabilidad de los modelos de árboles

# Modelo secuencial (notebook 06b)
torch>=2.2

# Utilidades
pyyaml>=6.0            # lectura de config/config.yaml
tqdm>=4.66             # barra de progreso al consolidar 40.336 archivos
jupyter>=1.0
